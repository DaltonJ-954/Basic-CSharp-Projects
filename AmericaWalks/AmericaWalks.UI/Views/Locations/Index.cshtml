@model IEnumerable<AmericaWalks.UI.Models.DTO.LocationDto>

@{
}

<h1 class="mt-3">Locations</h1>

<table>
    <thead>
        <tr>
            <th>Id</th>
            <th>Code</th>
            <th>WalkLocation</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var walk in Model)
        {
            <tr>
                <td>@walk.Id</td>
                <td>@walk.Code</td>
                <td>@walk.WalkLocation</td>
            </tr>
            }
    </tbody>
</table>